# === Core ===
ENV=development
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# === Database ===
# Local dev (Postgres en docker-compose o Postgres local)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cobranzasegurossuda
# Para SQLite local rápido (sin docker):
# DATABASE_URL=sqlite+aiosqlite:///./local.db

# === Superadmin (SIEMPRE en .env, nunca en DB) ===
SUPERADMIN_EMAIL=admin@voicenter.com.py
# Genera con: python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('TU_PASSWORD'))"
SUPERADMIN_PASSWORD_HASH=$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewdBPj/Tg2qOQbBO
SUPERADMIN_NAME=Administrador Voicenter

# === JWT ===
JWT_ALGORITHM=HS256
JWT_ACCESS_EXPIRE_MINUTES=60
JWT_REFRESH_EXPIRE_DAYS=7

# === Storage ===
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=20

# === Logs ===
LOG_LEVEL=INFO
AUDIT_RETENTION_DAYS=365

# === CORS ===
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# === Branding ===
BRAND_PRIMARY_COLOR=#0066B3
BRAND_LOGO_PATH=/logo-voicenter-color.png
